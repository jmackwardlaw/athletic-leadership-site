# Cloud Functions config for the AL Hub (NOT committed — copy to functions/.env).
# These are the real auth boundary, kept out of client code.

# Comma-separated Google Workspace domains allowed to use the hub.
ALLOWED_AUTH_DOMAINS=apps.anderson1.org

# Comma-separated staff emails that should receive the 'teacher' role.
TEACHER_EMAILS=wardlawj@apps.anderson1.org
